{
  "name": "typequest-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "TypeQuest -- Three.js/TypeScript rebuild spike (Phase 1: logic + content port behind tests)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
